import { BookOpen, Brain, BarChart3 } from 'lucide-react';\nimport clsx from 'clsx';\n\ntype Page = 'dashboard' | 'learn' | 'stats';\n\ninterface NavigationProps {\n  currentPage: Page;\n  onPageChange: (page: Page) => void;\n}\n\nexport function Navigation({ currentPage, onPageChange }: NavigationProps) {\n  const links: Array<{ page: Page; label: string; icon: typeof BookOpen }> = [\n    { page: 'dashboard', label: 'Главная', icon: BookOpen },\n    { page: 'learn', label: 'Обучение', icon: Brain },\n    { page: 'stats', label: 'Статистика', icon: BarChart3 },\n  ];\n\n  return (\n    <nav className=\"bg-white shadow-md border-b-4 border-indigo-600\">\n      <div className=\"max-w-7xl mx-auto px-4\">\n        <div className=\"flex justify-between items-center h-16\">\n          <div className=\"flex items-center gap-2\">\n            <BookOpen className=\"w-8 h-8 text-indigo-600\" />\n            <h1 className=\"text-2xl font-bold text-indigo-600\">Polyglot</h1>\n          </div>\n          <div className=\"flex gap-1\">\n            {links.map(({ page, label, icon: Icon }) => (\n              <button\n                key={page}\n                onClick={() => onPageChange(page)}\n                className={clsx(\n                  'px-4 py-2 rounded-lg font-medium flex items-center gap-2 transition-colors',\n                  currentPage === page\n                    ? 'bg-indigo-600 text-white'\n                    : 'text-gray-700 hover:bg-indigo-50'\n                )}\n              >\n                <Icon className=\"w-5 h-5\" />\n                {label}\n              </button>\n            ))}\n          </div>\n        </div>\n      </div>\n    </nav>\n  );\n}\n