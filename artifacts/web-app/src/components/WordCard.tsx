import { useState } from 'react';\nimport { motion } from 'framer-motion';\nimport { ChevronDown } from 'lucide-react';\nimport { type Word } from '../data/words';\n\ninterface WordCardProps {\n  word: Word;\n}\n\nexport function WordCard({ word }: WordCardProps) {\n  const [flipped, setFlipped] = useState(false);\n  const [showMnemonic, setShowMnemonic] = useState(false);\n\n  return (\n    <div className=\"space-y-4\">\n      <motion.div\n        initial={{ opacity: 0, y: 20 }}\n        animate={{ opacity: 1, y: 0 }}\n        transition={{ duration: 0.3 }}\n        onClick={() => setFlipped(!flipped)}\n        className=\"min-h-64 bg-gradient-to-br from-indigo-600 to-blue-600 rounded-2xl shadow-2xl p-8 cursor-pointer flex items-center justify-center text-white text-center relative overflow-hidden\"\n      >\n        <div className=\"absolute inset-0 opacity-10 bg-grid-pattern\"></div>\n        <div className=\"relative z-10\">\n          <div className=\"text-sm font-semibold opacity-75 mb-2\">Кликни чтобы перевернуть</div>\n          <div>\n            {!flipped ? (\n              <div>\n                <p className=\"text-sm opacity-75 mb-2\">Русский</p>\n                <p className=\"text-5xl font-bold\">{word.russian}</p>\n              </div>\n            ) : (\n              <div>\n                <div className=\"text-sm opacity-75 mb-2 flex flex-col gap-1\">\n                  <span>🇵🇱 {word.polish}</span>\n                  <span>🇩🇪 {word.german}</span>\n                  <span>🇬🇧 {word.english}</span>\n                </div>\n                <p className=\"text-3xl font-bold mt-4\">{word.english}</p>\n              </div>\n            )}\n          </div>\n        </div>\n      </motion.div>\n\n      <button\n        onClick={() => setShowMnemonic(!showMnemonic)}\n        className=\"w-full bg-white border-2 border-indigo-200 rounded-lg px-6 py-4 font-medium text-gray-700 hover:bg-indigo-50 transition-colors flex items-center justify-between\"\n      >\n        <span>💡 Мнемоника</span>\n        <ChevronDown className={`w-5 h-5 transition-transform ${showMnemonic ? 'rotate-180' : ''}`} />\n      </button>\n\n      {showMnemonic && (\n        <motion.div\n          initial={{ opacity: 0, height: 0 }}\n          animate={{ opacity: 1, height: 'auto' }}\n          exit={{ opacity: 0, height: 0 }}\n          className=\"bg-blue-50 border-2 border-blue-200 rounded-lg p-6\"\n        >\n          <p className=\"text-gray-700 leading-relaxed text-lg\">{word.mnemonic}</p>\n        </motion.div>\n      )}\n    </div>\n  );\n}\n